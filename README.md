# Telegram-бот "Епік-сервіс"

**Епік-сервіс** — це багатофункціональний Telegram-бот, розроблений на Python з використанням фреймворку Aiogram 3.x. Його головна мета — автоматизувати та спростити роботу зі складськими залишками.

---

## 🚀 Основні можливості

### Для користувачів:
* 🧠 **"Розумний" нечіткий пошук:** Знаходьте товари за артикулом або частковою назвою.
* 📋 **Формування списків:** Створюйте, переглядайте, зберігайте та скасовуйте списки товарів.
* ✏️ **Редагування списку:** Змінюйте кількість товарів у списку або видаляйте позиції.
* 📊 **Глобальний контроль залишків:** Система "на льоту" розраховує доступну кількість товару з урахуванням резервів.
* 💾 **Збереження та відстеження дефіциту:** При збереженні списку товари резервуються, а користувач отримує файл із замовленням.
* 🗂️ **Особистий архів:** Доступ до архіву всіх збережених списків.

### Для адміністраторів:
* 👑 **Адмін-панель:** Окремий розділ з розширеними правами.
* ✅ **Надійний імпорт/експорт:** Завантаження залишків з Excel, формування звітів.
* 🛡️ **Блокування дій:** Критичні операції блокуються при активних списках користувачів.
* 👥 **Архів користувачів:** Доступ до архівів списків усіх користувачів.
* 🗑️ **Повне очищення:** Функція для безпечного видалення даних.

---

## 🛠️ Технологічний стек

* **Python 3.10+**
* **Aiogram 3.x**
* **PostgreSQL** + **SQLAlchemy 2.x** (asyncpg)
* **Redis** (FSM Storage)
* **Alembic** (Migrations)
* **Pandas** / **Openpyxl**
* **TheFuzz**

---

## ⚙️ Встановлення та запуск

1.  **Клонуйте репозиторій:**
    ```bash
    git clone https://github.com/imeromua/epicservice.git
    cd epicservice
    ```

2.  **Створіть віртуальне оточення:**
    ```bash
    python3 -m venv venv
    source venv/bin/activate
    ```

3.  **Встановіть залежності:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Налаштуйте змінні оточення:**
    Скопіюйте приклад та заповніть своїми даними:
    ```bash
    cp .env.example .env
    nano .env
    ```
    *(Не забудьте вказати `BOT_TOKEN`, `ADMIN_IDS` та параметри БД)*

5.  **Ініціалізуйте базу даних:**
    ```bash
    # Створити файли міграцій (якщо запускаєте вперше)
    alembic revision --autogenerate -m "initial migration"

    # Застосувати міграції (створити таблиці)
    alembic upgrade head
    ```

6.  **Запустіть бота:**
    ```bash
    python3 bot.py
    ```

---

## 📞 Контакти

* **Email:** anubis.develop@gmail.com
* **Telegram:** @my_life_ukr
