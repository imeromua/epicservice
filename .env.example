# ============================================
# EpicService Configuration (.env)
# ============================================

# --- Telegram Bot ---
# Токен від @BotFather
BOT_TOKEN=your_bot_token_here

# Список Telegram ID адміністраторів через кому
# Отримати ID: @userinfobot
ADMIN_IDS=123456789,987654321

# --- PostgreSQL Database ---
DB_NAME=epicservice_bot
DB_USER=epicservice_bot
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=5432

# --- Redis (FSM Storage) ---
# true - використовувати Redis (рекомендовано)
# false - робота без Redis (пам'ять зникне після перезапуску)
REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# --- WebApp (Mini App / PWA) ---
# URL вашого домену (з https://)
# Приклад: https://epicservice.example.com
WEBAPP_URL=https://your-domain.com

# Список адмінів для WebApp (може відрізнятися від ADMIN_IDS)
# Якщо не вказано - використовується ADMIN_IDS
WEBAPP_ADMIN_IDS=123456789,987654321

# --- Логування ---
# DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# --- Інше (опціонально) ---
# Кількість архівних файлів на користувача (за замовчуванням 10)
# ARCHIVE_KEEP_COUNT=10

# Днів до видалення старих файлів з trash/ (за замовчуванням 14)
# TRASH_CLEANUP_DAYS=14

# ============================================
# Примітки:
# ============================================
# 1. Не комітьте .env файл в Git!
# 2. Копіюйте: cp .env.example .env
# 3. Замініть значення на реальні
# 4. Перезапустіть сервіси після змін
# ============================================
